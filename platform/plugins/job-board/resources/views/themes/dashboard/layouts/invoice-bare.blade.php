{{-- Minimal layout: only wraps the DataTable fragment (no sidebar/header). Used with JobBoardHelper::scope() + layout-settings. --}}
<div class="jb-invoice-table-fragment">
    @yield('content')
</div>
